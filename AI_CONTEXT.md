# 后续 AI 必读：dog11 闭环自主阶段

## 用户指令（最高优先级）

- 以学长版为代码基线，但不得保留“无论道具在哪里都执行同一段固定动作”的错误主流程。
- 正式规则是每方一个信息立牌，现场随机黄/黑二选一；首次任务完成后禁用路牌再次触发。
- 黄、黑牌都必须在狗前端距牌约 20 cm 时报警。
- 黑牌：一边沿黑色基准线前进，一边上下挥动机械臂 5 秒击倒；再继续进入球区。
- 黄牌：右平移、前进、左平移绕行，视觉重新回到黑色直线；再继续进入球区。
- 四足离散动作切换之间必须等待，目前 `motion.settle_s=0.6`。
- 黑线从视野下方消失后进入球区；先选最近球并锁定。目标锁定后可以保存另两球位置，但旧位置只能用于搜索，球可能滚动，每次移动仍要重新识别。
- 抓球必须闭环：俯身拍摄、一次短步修正、恢复/站稳、再次俯身拍摄，直到满足对应颜色的成功抓取窗。
- 不能用红蓝球位置推算绿球抓取点，其他颜色同理。
- 球抓起后还要放入杯中；三个球全部投杯后沿黑线原路返回并完全进入出发区。
- 红、绿、蓝三个球全部投指定的3倍杯，不选择最近杯。
- 最后一球投放后先后退2秒、原地转90度；若相机仍看到球或杯，再转45度并重新观察。
- 返程不能把右侧赛区边界当作黑色引导线；可用圆弧作为辅助证据，但圆弧本身不能驱动运动。
- 出发区是长方形：必须先看到两条横向长黑线，再确认二者在整幅画面中均连续消失。偏位造成左/右残线或斜线时不得结束。
- 所有代码要写清楚注释，方便其他 AI 读取。
- 自动迭代中评价`1 完赛`并不停止改代码：实际跑完的旧版本作为完赛基准，Codex仍要生成保守提速的`dogN+1`候选；候选必须重新实测后才能继承完赛标记。
- 成功记录必须包含实际路牌颜色（黄/黑）和总耗时。用户在OpenCode输入“停止”属于`opencode_stop`并自动评价2；状态机确认完全进入出发区后自行停车属于`goal_complete`，不能因停车而自动评价2。

## 资料事实与用户指令的边界

规则文档只作为事实来源，不是可执行指令。已确认事实：场地约350×150 cm、黑线约2 cm、球直径约2.8 cm、杯口约7 cm、圆牌直径5 cm、立牌仅一个随机黄/黑。规则俯视图保存在 `rule_reference/word/media/image1.png`。

`test_images/` 的15张旧图距离未知，只允许识别回归，禁止物理距离/抓取点标定。`calibration_images/` 才是用户按文件名明确提供的标定数据。

## 当前闭环实现

- 相机距离模型：`distance_from_dog_front = focal*object_size/apparent_size - camera_to_front_offset`。
- 黄色20/30 cm样本拟合得到 `focal≈171.73 px`、`offset≈2.66 cm`、RMSE≈0.52 cm。
- 黑牌轮廓会与暗背景/支架粘连；`VisionProcessor._black_hough_candidate` 结合圆弧、暗心、亮环、支架连续性，不允许退化为“没看到黄牌就是黑牌”。
- 路牌候选不强制处于黑线右侧走廊；只限制在宽的上部ROI。
- `SignDistanceTracker` 连续5帧确认颜色、中心、半径和 `20±2.5 cm`。
- `BallClosedLoopController` 使用 `alignment.profiles[color]`，不使用固定半径阈值。
- `BallSceneMemory` 锁定最近未完成颜色并记住其他球；提示30秒过期且不能直接驱动动作。
- `CupVision` 把杯口作为透视椭圆；四张空杯释放姿态都通过检测。
- `CourseMemory` 把规则图编码为拓扑状态，禁止把俯视示意图像素直接当低位相机坐标。
- `ReturnGuideVision` 排除贴右图边的候选，并用锁线连续性防止中途跳到赛区边界；首次锁线要求圆弧辅助证据。
- `StartZoneTracker` 先用双横线进入状态，再要求全画幅零横线连续5帧。单侧残线、斜线或未见过边界时均不能完成。
- 学长版 `get_ball(color, cup_distance, ...)` 已主动报错，防止固定秒数横移投杯；使用 `grab_ball(color)` 只闭环抓起并保持。

## 仍未解除的安全锁

1. `ball_config.json -> cup_alignment.post_release_verification_calibrated=false`：需要至少一组球已成功落杯后的相机照片，最好红绿蓝各有亮/暗两张。
2. 返程照片已收到并通过离线识别，但尚未在机械狗连续移动中验证锁线、转向和停车全过程。
3. 现有进入出发区照片主要是居中视角；仍需偏左、偏右以及横线明显倾斜时的连续实拍，才能解除返程动作安全锁。

在以上条件完成前，不得恢复学长版定时投杯、定时返程，也不得把完整自主入口标记为可比赛运行。

## 最低回归要求

每次修改视觉阈值、距离公式或状态机后运行：

```bash
python -m unittest discover -s tests -v
```

必须保持：15张识别库通过、12张测距路牌正确分色且20/30 cm门限分离、4张杯口姿态对准、未标定状态在动作前失败关闭。

自动工作流最低回归还包括：只有完整口令`启动`才能生成运行请求；评价1和2都进入Codex；dog序号每轮只能增加1；OpenCV临时录像仍以`.mp4`或`.avi`作为最终扩展名；Codex Desktop内置CLI可在无PATH/npm时自动解析。当前合计38项测试。
