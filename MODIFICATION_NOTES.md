# t2500：起扫行限制 + VOFA+ 修改说明

修改日期：2026-08-30

## 基线

本工程直接基于用户提供的 `D:\Desktop\t2500.zip` 修改。原始 ZIP SHA-256：

`99DC2CE301A68FC22024DBCA9A5F805603F388FEA73D1E46E129F3DDDE4C2465`

没有从此前的 NoRing 15、20、21 或其他交付包复制 PID、电机、圆环或相机控制代码。

## 本次修改

### 1. 横向十行扫线的起点下限位置

图像共有 120 行，合法行号为 0～119，行号从上向下增大。配置：

```c
#define HORIZONTAL_SCAN_BOTTOM_OFFSET (30U)
#define HORIZONTAL_SCAN_START_ROW_LIMIT \
    (IMG_ROW - 1U - HORIZONTAL_SCAN_BOTTOM_OFFSET)
```

因此当前限制行是 `119 - 30 = 89`。实际算法为：

```text
原起点 = 120 / 2 + 最长白列上端行 / 2
实际起点 = max(最长白列上端行, min(原起点, 89))
```

- 原起点不大于 89：完全沿用原算法。
- 原起点大于 89、且白列覆盖到第 89 行：从第 89 行开始。
- 最长白列上端行本身大于 89：从该白列上端开始，避免在白列之外起扫。
- 起点之后仍向下扫描 10 行，超过第 119 行的行号仍按原保护钳位到 119。

只改变横向边缘搜索的起始行；纵向最长白列搜索、阈值、边缘条件、参考列选择、十行数量和平均方法均未改变。

### 2. VOFA+ 调试输出

默认启用 UART0/P14.0、115200 baud、JustFloat，共 20 个通道，每 10 个控制帧发送一次。数据包含原始/PID 输入、实际有符号转向命令、左右电机最终百分比和 PWM、边缘有效度、最长白列上端行及实际起扫行。完整通道表见 `VOFA_PLUS_GUIDE.md`。

左右电机数据在原电机混合函数内、紧邻 `pwm_set_duty()` 前记录；正数表示正转，负数表示反转。`motor_stop()` 同时把全部电机遥测值清零。这些记录不改变 PWM 限幅或方向电平。

## 明确保留的 t2500 设置

- 屏幕关闭：`ENABLE_LCD_RUNTIME=0`。
- 20 秒停车关闭：`LAP_TIMED_STOP_ENABLE=0`。
- CPU1 中断/IDLE 唤醒关闭：`LOW_COMPUTE_CPU1_INTERRUPT_WAKE=0`，继续使用已验证的轮询握手。
- 左轮方向/PWM：P02.4 / P02.5；右轮方向/PWM：P02.6 / P02.7。
- 两轮方向电平：LOW 正转、HIGH 反转。
- PID：Kp=60、Ki=0、Kd=0，目标 188。
- 基础速度 15%，外轮增益上限 15%，内轮反转上限 30%，PWM 总限幅 50%。
- 没有增加十字路口专用识别、状态机或强制左右转逻辑。

## 构建

交付包不包含原 ZIP 中的 `Debug`、ELF、HEX、目标文件或其他旧构建产物。导入 AURIX Development Studio 后执行 Clean + Build，再烧录生成的新固件。
