# 新车电机适配说明

## 硬件定义来源

本版只从已经能在新车运行的 `znc_new.zip` 读取电机硬件定义。参考位置为 `znc_new/code/motor.h`，参考 ZIP SHA-256 为：

`4AACD808C884FD983F5CCF1124C45FF0FFF6C2D84BFA3329DD8E51635E27B0EB`

| 物理车轮 | 方向引脚 | PWM 引脚 | 正转 | 反转 |
| --- | --- | --- | --- | --- |
| 左轮 | P02.4 | ATOM0_CH5_P02_5 | LOW | HIGH |
| 右轮 | P02.6 | ATOM0_CH7_P02_7 | LOW | HIGH |

PWM 频率仍为 17 kHz。最大 50% 占空比、基础速度、PID 和转弯混合保持 t2500 基线不变；20 秒停车开关默认关闭。

本交付副本已删除 `cpu0_main.c` 中从未使用的 P21 旧板引脚宏，电机源文件中只保留上表所列的 `znc_new.zip` 有效引脚。该清理不会改变生成的电机控制行为。

## 没有移植的部分

`znc_new` 使用编码器、5 ms PIT 中断和速度 PI，并且需要按键启动；这些属于另一套控制架构，本版没有混入 NoRing 15。NoRing 15 仍在相机和双核初始化完成后按原流程直接控制电机，不需要按 `znc_new` 的 KEY1。

## 首次测试

1. 架空两个驱动轮后再上电。
2. 直行状态下确认两个轮子都向车头方向转。
3. 确认左转时左轮减速或反转、右轮加速；右转时相反。
4. 确认无图像或 CPU1 等待超时时会把两个 PWM 清零。

若某一侧方向仍与实车相反，请先停止测试并核对该侧电机线束，不要通过修改 PID 正负号来掩盖硬件方向问题。
