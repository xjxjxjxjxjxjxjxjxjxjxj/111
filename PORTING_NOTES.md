# TC264 圆环控制修改说明

> 本文件是基线工程遗留的历史圆环说明，其中部分参数已经过期。本次交付的准确信息以 `MODIFICATION_NOTES.md` 和当前源码宏为准；本次没有新增十字路口专用逻辑。

本工程的 PID 和圆环控制沿用 NoRing 15；本交付版另外按 `znc_new` 参考工程适配了新车电机引脚与方向极性。

## 保留参数

- PID：`Kp=60`、`Ki=0`、`Kd=0`，目标值 188
- 基础速度：15%，最长白列偏离中心时最低 12%
- 正常巡线始终使用 100% PID，不再削减为 20% 或 30%
- 左轮方向/PWM：`P02_4` / `ATOM0_CH5_P02_5`，LOW 正转
- 右轮方向/PWM：`P02_6` / `ATOM0_CH7_P02_7`，LOW 正转

## 缺口帧使用固定边界

缺口候选帧不再关闭 PID 直行，而是替换缺口一侧的十行边界平均值后立即交给原 PID：

- 左圆环缺口：左边界固定为 65 列，右边界继续使用实际识别平均值；
- 右圆环缺口：右边界固定为 123 列，左边界继续使用实际识别平均值。

因此车身轻微摇摆时仍会由 PID 修正，不会因为缺口判定帧连续直行而越走越偏。屏幕 `PZ` 是替换边界后的 PID 输入；`BA=1` 表示本帧启用了固定边界。

## 两个缺口的确认

第一缺口连续 2 帧后进入等待状态。等待第二个同向缺口期间：

- 第二缺口判断把最长白列允许范围放宽为中心 ±40 列；
- 最长白列短暂离开中心 ±25 列不会立刻取消，连续 12 帧不满足才取消；
- 第二缺口最大间隔仍按速度反比计算：`TO=80×15÷V`，并限制为 50～120 帧。

典型结果：`V=15 → TO=80`，`V=12 → TO=100`。十字专用判断仍保持删除状态。

## 当前圆环过程

```text
S0 正常 PID 巡线
 → 缺口候选帧：固定缺口侧边界并继续 PID
 → 连续 2 帧确认第一缺口，进入 S1 等待同向第二缺口
 → 确认第二缺口，进入 S2，按圆环方向第一次强制转弯 10 帧
 → 转弯结束立即进入 S3，恢复正常 PID 巡线
 → 入环至少 30 帧后，两侧连续 3 帧均几乎扫不到边界
 → 进入 S4，按相同方向第二次强制转弯 10 帧
 → S4 共保持固定边界辅助 50 帧；后 40 帧由原 PID 接管
 → 50 帧结束后回到 S0
```

两次强制转向方向完全相同。强转命令是当前速度的 85%，会随速度变化，并保证内侧轮仍为正转：

- `V=15`：强转命令 12，外轮约 27%，内轮约 3%；
- `V=12`：强转命令 10，外轮约 22%，内轮约 2%。

第二次强转及其后的 50 帧辅助阶段仍使用相同固定边界：左圆环固定左边界 65，右圆环固定右边界 123。两侧都暂时丢失时，另一侧使用对称备用值，使 PID 输入保持 188；一旦另一侧边界重新出现，PID 会立即利用实际边界修正方向。

## 屏幕圆环参数

| 显示 | 含义 |
| --- | --- |
| `S` | 状态：0 正常、1 等第二缺口、2 第一次强转、3 环内巡线、4 第二次强转/边界辅助 |
| `D` | 圆环方向：0 无、1 左、2 右 |
| `F` | 当前是否由强制转弯直接控制电机 |
| `GC/GE/CF` | 缺口候选 / 已确认缺口事件 / 候选连续帧数 |
| `C/Z` | 最长白列位置 / 原始左右边界平均和 |
| `W/A` | 等待第二缺口帧数 / 入环后经过帧数 |
| `L/R` | 左/右边界成功识别的行数 |
| `LL/RL` | 左/右边界最大连续丢失行数 |
| `E/ST/BF` | 第一次强转 / 第二次强转 / 第二阶段固定边界辅助帧数 |
| `BC` | 本帧两侧是否都几乎扫不到边界 |
| `V/O/RC` | 当前速度 / PID 转向命令 / 强转命令 |
| `BA` | 本帧是否启用固定边界 |
| `MC/ML/TO` | 最长白列是否在中心 ±25 / 连续偏离帧数 / 第二缺口超时帧数 |
| `LE/RE/PZ` | 实际左边界平均 / 实际右边界平均 / 固定边界处理后的 PID 输入 |
| `ZX` | 已执行强制转向的累计次数 |

导入 AURIX Development Studio 后建议先 Clean，再重新 Build。压缩包不包含旧 `Debug` 固件。

## 2026-08-19 图像边缘假边界修复

- 左边界平均值不大于 6，或右边界平均值不小于 181 时，即使有效行数较高，也按边界丢失处理；
- 已知左圆环方向且右侧丢失时，右边界补为 123；已知右圆环方向且左侧丢失时，左边界补为 65；
- 双边丢失判断同时检查有效行数和边界坐标，不再被图像边缘的假梯度欺骗；
- 圆环缺口要求对侧边界位于 65/123 的 ±35 列范围且没有贴边，防止大面积白场误判缺口；
- PID 输入距离目标值不超过 3 时归到目标值，`Z=185～191` 不再产生无意义的小幅转向。

例如 `L=0、LE=0、R=10、RE=186` 时，右侧虽然被旧扫描器记为 10 行有效，但新的控制层会把 186 判断为右边界丢失。普通状态下该画面不再触发圆环补线；若已经处于圆环的固定边界阶段，则左右边界分别使用 65 和 123，PID 输入为 188。

## 2026-08-20 强转提前恢复与出环补线巡线

本节以 `NoRing 10` 当前代码参数为准：固定左右边界为 50/138，和为 PID 目标188。

- 入环强转上限仍为20帧，出环强转上限仍为10帧；
- 入环和出环强转都必须至少执行5帧；前5帧即使双边界已经恢复，也不会提前进入正常巡线；
- 左右边界连续2帧均可靠恢复（各至少8行有效且不贴图像边缘）时，当前强转立即提前结束；
- 入环提前结束或达到20帧上限后，直接进入环内正常PID巡线，不启用补线；
- 出环提前结束或达到10帧上限后，进入35帧补线PID巡线，再结束圆环状态；
- 新增状态 `S=5` 表示出环后的补线PID巡线；
- 屏幕 `BR=1` 表示本帧左右边界均可靠恢复；出环时 `F=0、BA=1` 表示已经停止强转并由补线后的PID接管；
- 入环强转比例由170%提高到190%，出环由200%提高到220%。

原 PID 参数、入环与出环方向均未修改；电机引脚与方向极性按新车参考工程调整。
