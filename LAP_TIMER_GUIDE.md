# 20 秒计时停车说明

> 本次 t2500 交付版默认 `LAP_TIMED_STOP_ENABLE=0`，以下是以后需要手动启用该可选功能时的说明；当前烧录不会因运行 20 秒自动停车。

## 行为

- 上电和相机初始化阶段不计时。
- 第一次进入会实际驱动电机的控制分支时开始计时。
- 计时按真实经过时间连续累计；短暂丢线不会重新开始或暂停。
- 达到 20 秒时，程序先锁存停车，再跳过电机输出，清零 PWM 并复位 PID。
- 停车后不会自动重新启动；只有复位或重新上电才会开始下一次计时。

这是一项“运行 20 秒后停车”的功能，不是终点线或物理圈数识别。只有赛道单圈用时约为 20 秒时，它才等价于跑一圈后停车。

## 调整

宏位于 `user/cpu0_main.c` 顶部：

```c
#define LAP_TIMED_STOP_ENABLE        (0)
#define LAP_TIMED_STOP_SECONDS       (20U)
```

先把 `LAP_TIMED_STOP_ENABLE` 改成 1 才会启用计时。修改 `LAP_TIMED_STOP_SECONDS` 可以改变运行时间；改完后请执行 Clean + Build。保持为 0 会在编译时完全移除计时功能。

## 算力和安全

程序复用 STM0 的 64 位硬件计数，不增加新的定时器中断。每个控制帧只多两个寄存器读取、一次减法和一次比较，相比相机图像处理开销可以忽略。到时判断位于新 PWM 命令之前，即使相机或 CPU1 曾长时间延迟，恢复后也不会先短暂重新驱动车轮。
