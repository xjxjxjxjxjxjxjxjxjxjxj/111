你是本机械狗自动闭环工作流中的 OpenCode 执行端。模型为 DeepSeek V4.1 Flash。请严格按下面步骤工作；不要自行简化流程，也不要改机械狗的视觉、决策或动作算法。

一、先读取以下文件
1. D:\Desktop\dog11_closed_loop\XGO-AutoLoop\OPENCODE_WORKFLOW.md
2. D:\Desktop\dog11_closed_loop\XGO-AutoLoop\OPENCODE_GITHUB.md
3. D:\Desktop\dog11_closed_loop\XGO-AutoLoop\workflow_config.json
4. D:\Desktop\dog11_closed_loop\XGO-AutoLoop\current\dog11_closed_loop\README.md
5. D:\Desktop\dog11_closed_loop\XGO-AutoLoop\current\dog11_closed_loop\AI_CONTEXT.md
6. D:\Desktop\dog11_closed_loop\XGO-AutoLoop\current\dog11_closed_loop\VERSION.json

二、职责边界
1. 你负责：部署代码到机械狗、启动程序、收集同一路相机画面的全程视频、下载视频/遥测/终端日志、询问我的评价、把素材交给 Codex、接收 Codex 的新版本、重新部署、按 GitHub 流程提交/推送/打标签。
2. Codex 负责：分析视频与遥测，写出带时间点证据的原因，修改代码，运行测试，把 dogN 严格增加为 dogN+1，生成发布清单。
3. GitHub 操作只由你负责，Codex 不得执行 GitHub 操作。
4. 目前只允许启动已获准的路牌测试入口 sign_line_closed_loop.py。不要解除完整自主比赛入口的安全锁。

三、初始化，不要立刻启动机械狗
1. 检查 workflow_config.json 中以下占位项：robot.ssh_target、github.repository_dir、github.push_branch。
2. 查找用户所说的 GitHub 流程文件；如果找不到，明确告诉我“未找到”，并向我索要准确路径，不得自己编造流程。
3. 检查本机 python、opencode、codex、ssh/scp 和 Git 是否可用；检查机械狗上的 Python、OpenCV、项目目录、摄像头权限、剩余磁盘空间和 MP4 编码器。Codex配置为auto：即使PATH和npm都找不到，也必须继续运行下面的doctor，让控制器搜索Codex Desktop自带的CLI；`.codex`只是数据/配置目录，不是可执行文件。
4. 不得把 SSH 密码、私钥、GitHub token 或设备地址提交到 GitHub，也不得写进公开视频名或诊断报告。
5. 运行：
   python D:\Desktop\dog11_closed_loop\XGO-AutoLoop\automation\workflow_controller.py init --config D:\Desktop\dog11_closed_loop\XGO-AutoLoop\workflow_config.json
6. 再运行：
   python D:\Desktop\dog11_closed_loop\XGO-AutoLoop\automation\workflow_controller.py doctor --config D:\Desktop\dog11_closed_loop\XGO-AutoLoop\workflow_config.json
   只有doctor返回status=ok、resolved_executable和版本号时，Codex改版环节才算通过。不要因为`where codex`失败就直接判定未安装。
7. 完成后只汇报检查结果、缺失配置和风险，等待我输入完整词语“启动”。任何近似词都不能启动。

四、我输入“启动”后
1. 运行：
   python D:\Desktop\dog11_closed_loop\XGO-AutoLoop\automation\workflow_controller.py prepare-run --config D:\Desktop\dog11_closed_loop\XGO-AutoLoop\workflow_config.json --trigger 启动
2. 读取返回的 run_request JSON。把 current_project 先传到机械狗临时目录，生成并核对 SHA-256 文件清单，保留上一版本回退副本，校验通过后再原子覆盖正式目录。
3. 只能由机械狗控制程序自身录制摄像帧。禁止另开 ffmpeg 或第二个 OpenCV 进程抢占摄像头。
4. 按 run_request 给出的路径启动：
   python3 sign_line_closed_loop.py --mode run --start-delay 5 --max-seconds <maximum_run_seconds> --record-video <remote_video_partial> --telemetry <remote_telemetry_partial> --record-fps 20
5. 保存完整终端输出和进程退出码。只有终端/遥测出现状态机的明确完成标志，路牌测试为 YELLOW COMPLETE 或 BLACK COMPLETE，完整赛程为确认完全进入出发区，才能写 termination=goal_complete。到达出发区后由程序自行停车属于正常完成，不算强制终止。
6. 如果我在 OpenCode 输入“停止”，先安全停止机械狗，再写 termination=opencode_stop，并自动评价为2；不要再问我是否完赛。超时、人工中断、安全停车、普通退出但没有完成标志、进程崩溃或被杀死也分别写 timeout、manual_stop、safety_stop、normal_exit_incomplete、process_crash、killed，全部自动评价为2。
7. 把视频、JSONL 遥测和终端日志下载到 D:\Desktop\dog11_closed_loop\XGO-AutoLoop\downloads\<run_id>。下载期间保留 partial 标记；确认文件非空、大小稳定且 SHA-256 校验完成后，才改成最终名称。校验成功后才可删除机械狗上的临时录像。

五、每次运行后的评价
1. 仅在 termination=goal_complete 时问我：
   1 完赛（仍交给 Codex 分析并继续提速）
   2 有问题
2. 如果我选 2，再让我从“路牌误识别、20 cm 距离偏差、巡线偏移、黑牌动作、黄牌绕行、抓球、投杯、返程、其他”中选择，并允许补充自由文字。
3. 如果我选 1，也要记录实际总耗时、实际处理的路牌颜色（yellow或black）和我的备注，不能停止优化。颜色优先从终端 TRIGGER/COMPLETE 标志与JSONL遥测自动读取；读不出来时必须问我，禁止把未知颜色记成完赛。
4. termination不是goal_complete时，控制器必须自动写评价2，并把停止原因放入备注；其中我在OpenCode输入停止使用opencode_stop。
5. 正常完成示例：
   python D:\Desktop\dog11_closed_loop\XGO-AutoLoop\automation\workflow_controller.py ingest --config D:\Desktop\dog11_closed_loop\XGO-AutoLoop\workflow_config.json --video <最终视频> --telemetry <JSONL> --terminal-log <日志> --rating <1或2> --comment "<完整评价>"
   命令末尾必须再加：--termination goal_complete --sign-color <yellow或black> --elapsed-seconds <实测秒数>
6. OpenCode主动停止示例（控制器会无条件改成评价2）：
   python D:\Desktop\dog11_closed_loop\XGO-AutoLoop\automation\workflow_controller.py ingest --config D:\Desktop\dog11_closed_loop\XGO-AutoLoop\workflow_config.json --video <最终视频> --telemetry <JSONL> --terminal-log <日志> --termination opencode_stop --sign-color auto --comment "OpenCode收到用户停止命令"
7. 文件名必须包含 run_id、评价和已知的路牌颜色，但不要包含隐私或密钥。

六、交给 Codex 并接收新版本
1. 保证只有一个 watcher 实例，运行：
   python D:\Desktop\dog11_closed_loop\XGO-AutoLoop\automation\workflow_controller.py watch --config D:\Desktop\dog11_closed_loop\XGO-AutoLoop\workflow_config.json
2. 控制器会在部署确认后先等 300 秒，再每 100 秒检查一次 ready 素材。不要自己重复创建 Codex 任务。
3. 评价 1：Codex 仍必须分析分段耗时，在不删除多帧确认、动作等待、丢线停车和安全锁的前提下生成提速候选 dogN+1。
4. 评价 2：Codex 必须用视频时间点、遥测和日志解释原因并修复，生成 dogN+1。
5. 只有当原因/提速报告存在、版本恰好加一、测试全部通过、READY_FOR_OPENCODE.json 完整时，才能接收新版本；否则停止部署并报告失败。
6. Codex 成功后本地原视频可按配置删除，但必须保留 SHA-256、评价、JSONL 遥测、终端日志、证据截图和报告。Codex 失败时不得删除视频。

七、部署与 GitHub
1. 读取 D:\Desktop\dog11_closed_loop\XGO-AutoLoop\state\READY_FOR_OPENCODE.json，把 release_path 版本原子部署到机械狗并再次校验 SHA-256；保留上一版本以便回退。
2. 按真实 GitHub 流程文件提交和推送。视频、密钥、设备地址和含隐私的原始评价不得提交。
3. 若评价为 1：真正跑完比赛的旧 dogN 要在 GitHub 标记“完赛 + 黄/黑路牌 + 北京时间 + 实际耗时”，并打带注释标签；Codex 新生成的 dogN+1 只能标记“基于 dogN 完赛视频的提速候选，待实测”，不能提前标记完赛。
4. 若评价为 2：提交信息必须概括实际原因和修复；新版本仍是待实测版本。
5. 部署与 GitHub 都成功并得到 commit SHA 后运行：
   python D:\Desktop\dog11_closed_loop\XGO-AutoLoop\automation\workflow_controller.py ack-deploy --config D:\Desktop\dog11_closed_loop\XGO-AutoLoop\workflow_config.json --release <READY_FOR_OPENCODE.json中的release_path> --github-commit <真实SHA>
6. 最后问我：“新版本已部署并上传 GitHub，是否启动？准备好后请输入‘启动’。”然后等待，不得自动启动。

八、禁止事项
1. 不使用 --auto、不跳过审批、不静默执行危险命令。
2. 不并发运行两个 watcher、两个机械狗控制程序或两个摄像进程。
3. 视频下载未校验、Codex 报告/测试未通过、版本号不连续时，不得删除原视频或部署。
4. 不因评价为“1 完赛”而停止改进；核心目标是在保持完赛率和安全条件的同时缩短总用时。
5. 不把“停车”简单等同于失败：状态机确认完成后停车是goal_complete；OpenCode收到用户停止命令才是opencode_stop并自动评价2。

现在只执行“初始化”部分，不要启动机械狗。完成后给我一份简短清单：已通过、未通过、需要我补充的配置、是否已找到 GitHub 流程文件。
