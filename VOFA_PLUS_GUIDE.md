# VOFA+ 使用说明

本版沿用工程已经初始化的调试串口，不重复初始化串口，也不在中断中发送：

- UART0，115200 baud，8N1，无流控
- 单片机 TX：P14.0；RX：P14.1
- 协议：VOFA+ `JustFloat`

使用 3.3 V TTL USB 转串口模块。P14.0 接模块 RX，并连接公共 GND；只看波形时不需要连接 P14.1。不能连接 RS-232 电平接口。

## 通道定义

| 通道 | 内容 |
|---:|---|
| 0 | 已完成控制的帧计数 |
| 1 | CPU1 原始左右边界和 `current_z` |
| 2 | 边界辅助处理后的 PID 输入 `control_z` |
| 3 | 最终转向命令：左转为负，右转为正，直行为 0 |
| 4 | 左电机实际百分比：正数正转，负数反转 |
| 5 | 右电机实际百分比：正数正转，负数反转 |
| 6 | 左电机实际 PWM 计数：正数正转，负数反转 |
| 7 | 右电机实际 PWM 计数：正数正转，负数反转 |
| 8 | 十行扫线整体有效标志 |
| 9 | 左右边缘是否同时丢失 |
| 10 | 左边缘有效行数，范围 0～10 |
| 11 | 右边缘有效行数，范围 0～10 |
| 12 | 左边缘最大连续丢失行数 |
| 13 | 右边缘最大连续丢失行数 |
| 14 | 左边缘十行平均列 |
| 15 | 右边缘十行平均列 |
| 16 | 最长白列的上端行 `reference_boundary_row` |
| 17 | 本帧实际横向起扫行 `sample_start_row` |
| 18 | 最长白列参考列 `reference_col` |
| 19 | 最长白列参考是否有效 |

每包为 20 个 `float32` 加 4 字节 JustFloat 尾标，共 84 字节。第一帧发送一次，之后每 10 个已完成控制帧发送一次。发送点位于本帧电机命令写入之后，因此通道 3～7 表示本帧真正采用的命令；停车时这些通道均为 0。

115200 baud 下单包在线时间约 7.3 ms。默认降低发送频率以减少对巡线实时性的影响。需要更密或更疏的数据时，只修改 `user/cpu0_main.c` 顶部的 `VOFA_TELEMETRY_FRAME_DIVIDER`；数值越大，串口占用越低。将 `VOFA_TELEMETRY_ENABLE` 改为 0 可以在编译时移除全部 VOFA 打包与发送代码。

## VOFA+ 设置

1. 打开串口，选择对应 COM 口和 115200 baud。
2. 协议引擎选择 `JustFloat`。
3. 建立 20 条波形曲线，索引按上表从 0 开始。
4. 调试起扫行时重点观察通道 16、17：通常通道 17 是原白列中点；中点太靠下时为 89；白列上端大于 89 时，通道 17 跟随通道 16。
